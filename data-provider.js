/** Local-first player data provider for Mantra Lab. */
const LOCAL_LISTONE_URL='data/listone-2026-27.csv';
function parseCsv(text){const rows=[];let row=[],cell='',quoted=false;for(let i=0;i<text.length;i++){const ch=text[i],next=text[i+1];if(ch==='"'&&quoted&&next==='"'){cell+='"';i++;continue}if(ch==='"'){quoted=!quoted;continue}if(ch===','&&!quoted){row.push(cell);cell='';continue}if((ch==='\n'||ch==='\r')&&!quoted){if(ch==='\r'&&next==='\n')i++;row.push(cell);cell='';if(row.some(x=>x!==''))rows.push(row);row=[];continue}cell+=ch}if(cell!==''||row.length){row.push(cell);if(row.some(x=>x!==''))rows.push(row)}if(!rows.length)return[];const head=rows.shift().map(x=>x.trim());return rows.map(r=>{const o={};head.forEach((h,i)=>o[h]=r[i]??'');return o})}
function mantraRole(r){const s=String(r||'').trim().toUpperCase();return s==='POR'?'P':s}
function classicRole(r){const s=String(r||'').trim().toUpperCase();return({P:'POR',D:'DC',C:'CC',A:'ATT'}[s]||s)}
function normalizePlayer(o){return{id:String(o.id||o.Id),name:String(o.name||o.Nome||'').trim(),team:String(o.team||o.Squadra||'').trim(),classic:classicRole(o.classic||o.R),roles:String(o.roles||o.RM||'').split(/[|;]/).map(mantraRole).filter(Boolean),price:Number(o.price||o['Qt.A'])||0,initialPrice:Number(o.initialPrice||o['Qt.I'])||0,fvm:Number(o.fvm||o.FVM)||0,fvmMantra:Number(o.fvmMantra||o['FVM M'])||0,image:String(o.image||o.imageUrl||o.photo||o.photoUrl||'').trim(),season:String(o.season||'2026/27'),source:String(o.source||'Fantacalcio.it')}}
window.MantraLabDataProvider={parseCsv,async loadPlayers(){const response=await fetch(LOCAL_LISTONE_URL,{cache:'no-store'});if(!response.ok)throw new Error(`Listone locale non disponibile (${response.status})`);const rows=parseCsv(await response.text());const result=rows.map(normalizePlayer).filter(p=>p.id&&p.name);if(!result.length)throw new Error('Listone locale vuoto');window.MantraLabPlayers=result;return result}};